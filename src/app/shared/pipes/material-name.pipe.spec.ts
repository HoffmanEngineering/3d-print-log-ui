import { MaterialNamePipe } from './material-name.pipe';

describe('MaterialNamePipe', () => {
  it('create an instance', () => {
    const pipe = new MaterialNamePipe();
    expect(pipe).toBeTruthy();
  });
});
